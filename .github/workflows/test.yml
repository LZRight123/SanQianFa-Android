on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      - run: pwd
        name: 打印当前目录
      - name: 打印当前目录下的文件
        run: ls -la
